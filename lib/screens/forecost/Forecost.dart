import 'package:flutter/material.dart';

class Forecost extends StatefulWidget {
  Forecost({Key key}) : super(key: key);

  @override
  _ForecostState createState() => _ForecostState();
}

class _ForecostState extends State<Forecost> {
  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
